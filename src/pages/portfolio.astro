---
import BaseLayout from "../layouts/BaseLayout.astro"

const pageTitle = "Portfolio - under development!"

const profile = {
    firstName: "Helen",
    state: "California",
    country: "United States",
    year: "3rd",
    major: "Computer Science",
    school: "University of California, Los Angeles",
    hobbies: ["drawing", "badminton", "listening to music"]
}

const proglangs = ["Python", "JavaScript/Typescript", "C++/C", "HTML", "CSS", "SQL"]
const skills = ["React", "Flask", "GraphQL", "Linux", "Python ML libraries (sklearn, PyTorch)", "OpenGL", "OpenCV"]
const tools = ["Qt", "Postman", "Version Control (Git/GitHub/GitLabs)", "Bash", "AWS"]

---

<BaseLayout pageTitle={pageTitle}>
    <h2>Helen Feng</h2>
    <h3>Introduction</h3>
    <ul>
        <li>My name is {profile.firstName}.</li>
        <li>I live in {profile.state}, {profile.country} and I'm currently an upcoming {profile.year} year at {profile.school}.</li>
        {profile.hobbies.length >= 2 &&
            <li>Two of my hobbies are: {profile.hobbies[0]} and {profile.hobbies[1]}</li>
        }
        <li>I am fluent English and Chinese (Mandarin)! I'm also trying to learn Japanese.</li>
    </ul>
    <h3>Skills</h3>
    <p>Here are the programming languages I use most often</p>
    <ul>
        {proglangs.map((lang) => <li>{lang}</li>)}
    </ul>
    <p>Here's a list of frameworks/libraries I'm familiar with:</p>
    <ul>
        {skills.map((skill) => <li class="skill">{skill}</li>)}
    </ul>
    <p>And here are some tool's I've worked with:</p>
    <ul>
        {tools.map((tool) => <li>{tool}</li>)}
    </ul>

    <h2>Stay tuned for more information about projects I am working on or have completed!</h2>
</BaseLayout>